import dashboard from './dashboard';
import invoice from './invoice';
import masters from './masters';
import others from './others';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, invoice, masters, others]
};

export default menuItems;
